import 'package:flutter/material.dart';

class MyJigsPage extends StatelessWidget {
  const MyJigsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("나의 지그 화면입니다"),
    );
  }
}